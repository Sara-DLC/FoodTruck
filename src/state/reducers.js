import * as types from './actionTypes';

const initialTrucks = []

export const trucksReducer = (state = initialTrucks, action) => {
    switch(action.type){
        default:
            return state
    }
}